import AdminDashboard from "./AdminDashboard";

export const metadata = {
  title: "Admin — STL Mold Removal Leads",
  robots: "noindex, nofollow",
};

export default function AdminPage() {
  return <AdminDashboard />;
}
